{
  "name": "@slotnova/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "Slotnova transactional outbox consumer and separate pg-boss foundation scheduler.",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "@slotnova/observability-server": "workspace:*",
    "pg-boss": "12.31.1",
    "@slotnova/db": "workspace:*",
    "@slotnova/api": "workspace:*"
  },
  "devDependencies": {
    "@slotnova/tsconfig": "workspace:*"
  }
}
