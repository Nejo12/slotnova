{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["SLOTNOVA_ENV", "VITE_*"],
      "outputs": ["dist/**", ".output/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test:integration": {
      "dependsOn": ["^build"],
      "outputs": [],
      "cache": false
    },
    "tokens:generate": {
      "outputs": [],
      "cache": false
    },
    "contracts:generate": {
      "outputs": [],
      "cache": false
    },
    "db:migrate": {
      "passThroughEnv": ["SLOTNOVA_ENV", "DATABASE_*", "NODE_EXTRA_CA_CERTS"],
      "dependsOn": ["build"],
      "outputs": [],
      "cache": false
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true,
      // Local-dev env vars (never secrets — see docs/runbooks/local-dev.md).
      // Turbo's default strict env mode otherwise drops these before they
      // reach `tsx watch` / `vite`.
      "env": [
        "SLOTNOVA_ENV",
        "NODE_EXTRA_CA_CERTS",
        "DATABASE_CONNECTION_MODE",
        "DATABASE_URL",
        "WORKER_*",
        "DATABASE_SSL",
        "DATABASE_POOL_MAX",
        "DATABASE_CONNECT_TIMEOUT_MS",
        "DATABASE_IDLE_TIMEOUT_MS",
        "DATABASE_STATEMENT_TIMEOUT_MS",
        "DATABASE_APP_NAME",
        "API_*",
        "VITE_*"
      ]
    }
  }
}
