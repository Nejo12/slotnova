{
  "name": "@slotnova/observability-browser",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "Slotnova browser error-reporting seam: provider-neutral ErrorReporter port with release/environment context and a pluggable sink. Browser-safe; no server observability dependency; no vendor SDK.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@slotnova/tsconfig": "workspace:*"
  }
}
